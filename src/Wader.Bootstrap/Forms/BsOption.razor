@inherits Wader.Bootstrap.Internal.BaseComponents.BsChildContentComponent
@typeparam TValue
<option @attributes="RenderAttributes" value="@(ValueAsString)">
    @if (ChildContent is not null)
    {
        @(ChildContent)
    }
    else
    {
        @(ValueAsString)
    }
</option>
