@inherits Wader.Bootstrap.BaseComponents.BsChildContentComponent
<div role="progressbar" aria-valuenow="@(ValueNow)" aria-valuemin="@(ValueMin)" aria-valuemax="@(ValueMax)" style="@(WidthStyle)" @attributes="RenderAttributes">
    <CascadingValue Name="@(nameof(BsProgress))" Value="ActualWidth">
        @(ChildContent)
    </CascadingValue>
</div>
