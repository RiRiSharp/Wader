@inherits Wader.Bootstrap.Internal.BaseComponents.BsChildContentComponent
<div @ref="HostElementRef" @attributes="RenderAttributes">
    @(ChildContent)
</div>

@* These wrapper elements only exist to render the sent render fragments, without showing them to the user *@
@if (BsPopoverTitle is not null)
{
    <div class="d-none" data-wd-remove-wrapper="true" aria-hidden="true" @ref="_titleRef">
        @(BsPopoverTitle)
    </div>
}

@if (BsPopoverContent is not null)
{
    <div class="d-none" data-wd-remove-wrapper="true" aria-hidden="true" @ref="_contentRef">
        @(BsPopoverContent)
    </div>
}
