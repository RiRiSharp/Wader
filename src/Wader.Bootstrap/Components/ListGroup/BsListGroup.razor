@inherits Wader.Bootstrap.BaseComponents.BsChildContentComponent

@switch (Type)
{
    case BsListType.ContentDivision:
        <div @attributes="RenderAttributes">
            @(ChildContent)
        </div>
        break;
    case BsListType.UnorderedList:
        <ul @attributes="RenderAttributes">
            @(ChildContent)
        </ul>
        break;
    case BsListType.OrderedList:
        <ol @attributes="RenderAttributes">
            @(ChildContent)
        </ol>
        break;
    default:
        throw new ArgumentOutOfRangeException();
}